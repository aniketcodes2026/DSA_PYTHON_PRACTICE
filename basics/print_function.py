#to write all natural number till n without spacing and in one line

    n = int(input())
    list=[]
    i=1
    while i = n
        list.append(i)
        i+=1
    result=''.join(map(str,list))
    print(result)